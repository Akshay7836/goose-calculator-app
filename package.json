{
  "name": "goose-calculator-app",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --mode development",
    "build": "tsc && vite build --mode production",
    "preview": "vite preview --mode production",
    "lint": "tsc --noEmit",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vite": "^4.3.0"
  },
  "dependencies": {},
  "author": "Your Name",
  "license": "MIT",
  "description": "A calculator application built with Vite and TypeScript"
}
